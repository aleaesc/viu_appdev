{
  "name": "viu-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless backend for VIU frontend using Vercel + Firebase",
  "scripts": {
    "build": "echo \"No build step for serverless JS\"",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "zod": "^3.23.8"
  }
}
